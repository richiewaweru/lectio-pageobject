# Auth via environment only — never commit tokens.
# NODE_AUTH_TOKEN is set by CI / local secrets when publishing.
registry=https://registry.npmjs.org/
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
